name: static analysis

on:
  pull_request:

jobs:
  static-analysis:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Fetch all
        run: git fetch

      - name: Guard
        run: ./git-guard.sh ${{ github.head_ref }} develop
